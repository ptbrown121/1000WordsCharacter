{
  "name": "1000words-card-manager",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Character & dice-pool manager for the 1000 WORDS TTRPG. Zero-dependency, no build — open index.html to run. This file only enables `node --test` for the pure logic in js/pool.js.",
  "scripts": {
    "test": "node --test \"test/**/*.test.js\""
  }
}
